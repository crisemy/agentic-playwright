# .github/workflows/ci.yml
name: Playwright Async + Agentic Crew CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    name: Tests + Agentic Swarm (${{ matrix.browser }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install ${{ matrix.browser }} --with-deps

      - name: Create directories
        run: mkdir -p screenshots auth reports

      - name: Run Classic Async pytest Suite (parallel)
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          HEADLESS: true
          SLOW_MO: 0
        run: |
          pytest -n auto --browser ${{ matrix.browser }} --html=reports/pytest-${{ matrix.browser }}.html -q

      - name: Run Agentic CrewAI Swarm
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          HEADLESS: true
          SLOW_MO: 0
        run: |
          python -m agents.testing_crew

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: full-report-${{ matrix.browser }}
          path: reports/
          retention-days: 14